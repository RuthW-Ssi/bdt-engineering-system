# QA Findings — MO Print Packet + DWG Removal (PDF-only) + WoVisualTab split fix

- **feature:** MO Print Packet (new) · Drawing DWG removal, `.pdf`-only (removal) · WoVisualTab 3D/Drawing split (UX tweak)
- **task:** No Notion task — same-day fast-tracked conversational work (precedented same day by PR #176, #178). Step 1 "Notion DoD check" skipped per explicit task instruction; `wiki/features/drawing.md`'s `### 2026-09-15 removal` section used as the tester-equivalent artifact instead.
- **branch under review:** `dev-t-mo-print-packet` — **UNCOMMITTED working tree** (pre-commit `/release-gate`-style run), based off `dev` @ `9eaa78f`. Diffed against `dev`.
- **reviewer:** qa (this run)
- **date:** 2026-09-15
- **scope of this review:** QA lens only (implementation-matches-wiki / regression evidence / dead-code / build+test health / doc alignment). No security subagent dispatched in this run — not requested as part of this task; should be confirmed separately before shipping if not already run in parallel elsewhere.

## QA-01 — Stale comment in `DrawingList.tsx` still describes a browse-side DWG/PDF toggle that Round 3b removed

- **where:** `src/pages/DrawingList.tsx`, the doc comment immediately above `export function filterDrawingsByType`
- **what:** The comment reads: *"Upload is .pdf-only since 2026-09-15's DWG removal, but legacy .dwg files uploaded before that still exist (stored/downloadable, no preview) — **this toggle lets the browse side view either stream, independent of upload.**"* This describes the DWG/PDF `<select>` toggle that Round 3 kept and Round 3b (same day, later in the session) then removed — the diff shows the `<select>` and its `fileType` state are gone; `drawingsList` is now hardcoded to `filterDrawingsByType(drawingsListRaw, 'pdf')`. The comment was written for the Round-3 state and never updated when Round 3b shipped, so it now describes a UI control that no longer exists — misleading to a future reader who hasn't also read the wiki's Round 3b note.
- **severity:** Low
- **evidence:** `git diff dev -- src/pages/DrawingList.tsx` — the retained comment block vs. the same diff's removal of the `<select aria-label="File type">` block and the `fileType`/`useEffect` state. Compare against `wiki/features/drawing.md`'s "Round 3b, same day" paragraph, which correctly documents the toggle as removed.
- **fix_route:** fe — trim the comment to drop the "this toggle lets..." sentence (e.g. "...still exist (stored/downloadable, no preview, no longer browsable through this page — see Round 3b)."). Purely cosmetic; no behavior is affected since `filterDrawingsByType` itself is correct and tested.

## QA-02 — `drawings.controller.ts` / `manufacturing-orders.controller.ts` (incl. new `print-packet` route) have 0% direct test coverage — confirmed pre-existing, repo-wide pattern, not a regression

- **where:** `backend/src/modules/drawings/drawings.controller.ts`, `backend/src/modules/manufacturing-orders/manufacturing-orders.controller.ts` (coverage-summary.json, this run's `npx jest --coverage` output)
- **what:** Role-card coverage target is 80%+ for controllers; both sit at 0% lines/statements/branches. This includes the new `GET /mo/:id/print-packet` route. Checked whether this is new debt from this branch: sampled `coverage-summary.json` across every `*.controller.ts` in the repo (29 total) — 28 of 29 are at 0%, the sole exception being `file-storage.controller.ts` (46.66%, has a dedicated `file-storage.controller.spec.ts`). This is this codebase's established convention (business logic tested via services — `mo-print.service.ts` 100%, `drawings.service.ts` 100%, `mark-drawing-match.ts` 100%, `mo-print-pdf-builder.ts` 97.4% lines, `create-drawing.dto.ts` 100% — all meet or exceed the 90%/100% service/DTO targets), not a gap introduced by this PR.
- **severity:** Low / Info
- **evidence:** `cd backend && npx jest --coverage --coverageReporters=json-summary` then inspecting `coverage/coverage-summary.json` for every `*.controller.ts` key — 28/29 at `lines.pct: 0`, only `file-storage.controller.ts` non-zero. Touched services/DTOs individually confirmed ≥97.4% lines, meeting role-card targets.
- **fix_route:** none required for this PR. If the repo ever decides to raise controller-coverage as a standing bar, that's a cross-cutting backlog item, not specific to this bundle.

## QA-03 — "46 legacy `.dwg` rows deleted, scoped to exactly `project_id=7`" is a data-layer claim not independently re-verified via DB query

- **where:** `wiki/features/drawing.md`'s `### 2026-09-15 removal` section, Round 3b bullet: *"All 46 legacy `.dwg` rows/files for the one project that had them were deleted for real (DB row + storage file each) via the app's own `DELETE /drawings/:id` endpoint (looped, not a raw DB delete), per explicit user confirmation... verify no other project's `.dwg` data was touched (should be scoped to exactly project_id=7)."*
- **what:** This is a one-time data-mutation action already performed live during the session (not part of the uncommitted code diff — nothing in `git diff` touches data), so there is no code artifact to review it against. Per this repo's own convention (QA is review-only; direct DB queries/mutations against dev data are confirmed with the user first, not run unilaterally), this review did not run a DB query (e.g. `SELECT project_id, count(*) FROM drawing WHERE file_name ILIKE '%.dwg' GROUP BY project_id`) to independently confirm the scoping. The described mechanism (looped calls to the app's own guarded `DELETE /drawings/:id` endpoint, not a raw multi-row SQL statement) is inherently harder to over-scope than a bulk delete, which somewhat mitigates the risk, but this review is not first-hand confirmation.
- **severity:** Low / Info
- **evidence:** No `drawing`-table rows/deletes appear in `git diff dev --stat` (expected — this was a live data action, not a migration). No DB query was run in this review pass.
- **fix_route:** none required to ship (code-diff-level review is otherwise clean). If the user wants independent confirmation, a one-line `SELECT DISTINCT project_id FROM drawing WHERE file_name ILIKE '%.dwg'` (should return only `project_id=7` or nothing, if all 46 were in fact scoped and deleted) would close this out — optional, not blocking.

## Independent verification performed in this pass (not findings — confirms claims accurate)

- **Backend tests:** `cd backend && npx jest` → **707 total, 690 passed, 17 failed across 4 suites** (`cycle-time.service.spec.ts`, `template-binding.service.spec.ts`, `project-progress.service.spec.ts`, `bom-matching.service.spec.ts`) — matches the wiki's claimed count exactly. Confirmed via `git diff dev --stat` that none of these 4 spec files (or their corresponding service files) are touched by this branch. All touched-file suites pass clean: `drawings.service.spec.ts`, `mo-print/mark-drawing-match.spec.ts`, `mo-print/mo-print-pdf-builder.spec.ts`, `mo-print/mo-print.service.spec.ts`, `file-storage/drivers/local.driver.spec.ts`, `file-storage/drivers/gcs.driver.spec.ts`, `file-storage.controller.spec.ts`.
- **Frontend tests:** `npx vitest run` (repo root) → **8 files, 60/60 passed**, 0 failures — matches claim exactly.
- **Backend build:** `cd backend && pnpm run build` (`nest build`) → clean, no errors.
- **Frontend build:** `pnpm run build` (root, `tsc -b && vite build`) → clean; only a pre-existing "chunk larger than 1000kB" advisory warning (unrelated to this branch, not an error).
- **High-risk spot-check 1 — Dockerfile font-shipping:** `git diff dev -- backend/Dockerfile` shows `COPY --from=builder /app/assets ./assets` added to the runtime stage. Confirmed `backend/assets/fonts/sarabun/{Sarabun-Regular.ttf,Sarabun-Bold.ttf,Sarabun-OFL.txt}` exist on disk and are untracked-but-present (new files, will be added on commit). Without this line the vendored Thai font would not ship to the Cloud Run image and every Thai project/zone name would 500 the print-packet endpoint in staging/prod — line is present and correctly placed in the final runtime stage (after the `builder` COPY of `node_modules`/`dist`).
- **High-risk spot-check 2 — `getLatestVersion` `.pdf`-only filter:** `git diff dev -- backend/src/modules/drawings/drawings.service.ts` — `fileType` param dropped from the signature, but the Prisma `where` clause is hardcoded to `file_name: { endsWith: '.pdf', mode: 'insensitive' }` (was a parameterized `.${fileType}`). Confirmed this is a real, hardcoded filter, not accidentally dropped — legacy `.dwg` rows' version numbers cannot leak into a new `.pdf` upload's counter.
- **High-risk spot-check 3 — server-side `.pdf`-only enforcement:** `git diff dev -- backend/src/modules/drawings/dto/create-drawing.dto.ts` — `@Matches(/\.pdf$/i, { message: '...' })` added directly on `file_name`, case-insensitive, end-anchored. `Matches` is correctly imported from `class-validator` (confirmed import line). A direct `POST /drawings` with a `.dwg` (or any non-`.pdf`) `file_name` is rejected by validation before reaching the service — this is real server-side enforcement, not just a UI restriction.
- **Dead-code grep:** `grep -rn "DrawingApsService\|DrawingApsPreview\|useDrawingApsStatus\|useDrawingApsViewerToken\|LatestVersionQueryDto" backend/src src` → 0 hits (files fully deleted, no stragglers). `grep -rn "findLatestDwgForMark"` → 2 hits, both explanatory comments (`WoVisualTab.tsx`'s own doc comment, `mo-print/mark-drawing-match.ts`'s doc comment referencing the frontend function it ports), not live code. `aps_urn`/`aps_translation_status`/`aps_translation_error` → only appear in `backend/prisma/schema.prisma` (deliberately kept, per immutable-migrations convention, `CLAUDE.md` §7) — zero code reads/writes anywhere in `backend/src` or `src`.
- **`ApsClientService`/`ApsModule` isolation:** `git diff dev -- backend/src/modules/aps/aps-client.service.ts` shows only the dead `drawingBucketKey` getter removed + comment updates — `bucketKey`, `getAccessToken`, `getSignedDownloadUrl`, `translate`, etc. all unchanged. `git diff dev --stat` confirms zero changes to `backend/src/modules/bim/` (`BimService`, `BimBackupService`) — BIM's APS usage is untouched.
- **`ProgressDrawingPanel.tsx` / `MobileDrawingSheet.tsx` reuse:** `ProgressDrawingPanel.tsx`'s `markMatches` now filters `list.filter(d => d.file_name.toLowerCase().endsWith('.pdf') && fileMatchesMark(...))` before taking latest version — confirmed. `MobileDrawingSheet.tsx` is not in the diff at all (0 changes), consistent with the wiki's claim that it reuses `ProgressDrawingPanel` unchanged and needed no direct edit.
- **`WoVisualTab.tsx` split ratio:** `git diff dev -- src/components/wo/WoVisualTab.tsx` — both panes changed from `flex-[1.6]`/`flex-1` to `flex-1`/`flex-1`, comment updated accordingly. Matches the claimed 50/50 landing point.
- **MO Print Packet route wiring:** `manufacturing-orders.module.ts` imports `DrawingsModule` + `FileStorageModule` and provides `MoPrintService`; `drawings.module.ts` now `exports: [DrawingsService]` (required for `MoPrintService`'s constructor injection to resolve — confirmed present, would otherwise be a DI failure caught by `pnpm run build`/tests, neither of which failed). Controller route `GET :id/print-packet` uses plain `@Res()` (no passthrough) with `Buffer.from(bytes)` — confirmed exceptions thrown inside `buildPdf()` (`NotFoundException` for a missing MO, `ConflictException` for zero WOs or any WO missing a matched `.pdf`) are regular Nest exceptions thrown before the `@Res()` write, so Nest's exception filters still catch them normally; only the success path bypasses passthrough serialization. No two-segment vs. one-segment route-matching conflict with the existing `GET :id` handler.
- **`fetchMoPrintPacketBlob` error re-parse:** `src/api/mo.ts` — confirmed the `catch` block checks `isAxiosError(err) && err.response?.data instanceof Blob`, reads it via `.text()`, and reassigns `err.response.data = JSON.parse(text)` so `getErrorMessage` downstream sees the real 409 message instead of a generic blob-shaped fallback; falls through safely (leaves the blob in place) if the body isn't JSON.
- **Nothing accidentally staged:** `git diff --cached --stat` is empty — no files staged. `git status --short` shows the expected tracked-file changes (34 files, matches `git diff dev --stat`) plus a long tail of pre-existing untracked debris consistent with this repo's documented convention (`backend/*_tmp.*`, `backend/prisma/seed-*.ts`/`patch-*.ts`, `document/backup_knowledge/*`, `storage/`), none of which is staged or part of this diff.
- **Wiki alignment:** `wiki/features/drawing.md`'s `### 2026-09-15 removal` section read in full — coherent, internally consistent (Round 1 → 2 → 3 → 3b escalation matches the actual diff exactly), correctly cross-links the `MoPrintService.buildPdf` root-cause connection, and the `## Data model`/`## API`/`## Frontend` sections below it were also checked and are consistent with the removal (aps-status/aps-viewer-token routes shown struck through with a removal note, `drawing.aps_urn` block marked "Dead since 2026-09-15").

## Verdict

**PASS** — 3 findings, all Low/Info, zero Medium/High/Critical. All three named high-risk spot-checks (Dockerfile font-shipping, `getLatestVersion` `.pdf` filter, `.pdf`-only `@Matches` enforcement) pass. Both test suites pass with only the same pre-existing, unrelated 17-test/4-suite baseline failure (confirmed by diff, not just by count). Both builds clean. Dead-code grep is clean (2 remaining hits are explanatory comments, not live references). `ApsClientService`/BIM isolation confirmed untouched. Wiki is updated and internally consistent with the diff. No user confirmation needed to ship — see sign-off.
