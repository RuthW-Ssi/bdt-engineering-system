# QA Findings — Drawing PDF Upload + Preview + Per-Type Versioning

- **feature:** F-Drawing PDF Upload + Preview (https://app.notion.com/p/3dbaa61b71f6813c8e17d1b356c9bf0e)
- **task:** T01 — PDF upload/preview + per-type versioning + blob-URL revoke bugfix (https://app.notion.com/p/3dbaa61b71f68193adbbf714706bbdcf), Sprint 35
- **branch under review:** `dev-t-drawing-pdf-upload` — **UNCOMMITTED working tree** (pre-commit `/release-gate` run); diffed against `dev` (not `main`), per task framing. Not pushed to `origin` (confirmed: `git branch -r` has no `origin/dev-t-drawing-pdf-upload`).
- **date:** 2026-09-14
- **reviewer:** qa (this run)
- **scope of this review:** QA lens only (release-readiness / DoD-coverage / doc-alignment / regression evidence). No security subagent run requested/observed in this dispatch — if `/release-gate`'s normal parallel security pass hasn't happened separately, that should be confirmed before shipping (not re-litigated here, out of this run's requested scope).

## QA-01 — `wiki/features/drawing.md` not updated for this increment; current-state banner still ends at "DWG-only" (2026-08-26)

- **where:** `~/Documents/bdt/knowledge-base/projects/bdt-engineering-system/wiki/features/drawing.md`
- **what:** The page's top status banner (the reader's "what's current" summary) walks the feature's history up through `> **DWG-only + APS 2D preview shipped 2026-08-26**...` and stops there — no entry for this increment (PDF reintroduced as a second, independently-versioned format via a type picker). A reader skimming just the banner (the doc's own intended fast-path) would wrongly conclude the feature is still DWG-only. The `git log`/`git status` for the knowledge-base repo confirms this file has zero uncommitted changes and its last commit (`139822a`) is the 2026-08-26 increment — it has not been touched for this work. Note the body of the page *does* still contain old `.pdf → native <iframe>` text (line ~152), but that sits inside the dated `2026-08-24 DXF preview + tools` historical subsection and is legitimately scoped as history, not current-state — the actual gap is the missing current-state banner entry, not stale history.
- **severity:** Medium
- **evidence:** `grep -n "2026-09-14\|PDF upload\|pending commit" wiki/features/drawing.md` → 0 hits. `cd ~/Documents/bdt/knowledge-base && git log --oneline -1 -- projects/bdt-engineering-system/wiki/features/drawing.md` → `139822a docs: Drawing DWG-only + APS 2D preview (2026-08-26 increment)`; `git status --short` shows this file **not** in the modified list. This is structurally the same gap class QA-02 raised for the 2026-08-26 increment (`docs/qa/findings/2026-08-26-drawing-aps-preview.md`) — an expected, recurring per-increment gap (wiki-integrator runs post-`/release-gate`, per this project's own cascade design in `CLAUDE.md` §5.2 step 6.1), not yet closed for *this* increment.
- **fix_route:** wiki-integrator — add a dated entry to the status banner + a `## 2026-09-14 — PDF upload + preview` body section (DWG/PDF type picker, per-type version scoping, iframe PDF preview, the blob-URL revoke bugfix), matching the pattern of the existing dated subsections.

## QA-02 — Wiki test summary's global "Coverage achieved" / "Known gaps" sections now self-contradict the new 2026-09-14 increment section in the same file

- **where:** `wiki/tech/testing/per-feature/drawing.md`, lines ~109–114 ("Coverage achieved") and ~121–125 ("Known gaps / TBD", the undated/global section at the bottom of the file)
- **what:** These sections still state "Frontend: no dedicated Vitest suite (this codebase's established convention for thin React Query hook/UI wiring...)" and "No frontend unit tests exist for the new Zone/Sub-zone picker logic in `DrawingList.tsx` — covered only by live Playwright + `tsc`". Both are now false as a blanket statement: the 2026-09-14 increment section *directly above* in the same document reports 4 new Vitest files / 17 new/changed tests (confirmed by this review, see "Independent verification" below) — the "no dedicated Vitest suite" convention this feature had followed since 2026-08-25 was deliberately broken for this increment (TDD per the Notion completion notes), but the older global sections were never updated to reflect that shift. A reader jumping to "Coverage achieved" without reading the full increment history above would get a materially wrong picture of current frontend test coverage.
- **severity:** Medium
- **evidence:** `wiki/tech/testing/per-feature/drawing.md:114` — `"Frontend: no dedicated Vitest suite..."` vs. the same file's line 27 (2026-09-14 increment row) — `"Frontend: 52/52 passing (was 50 before the navpanes+regression tests)"`. Confirmed 4 real Vitest files exist and pass (`src/hooks/useDrawings.test.tsx`, `src/components/drawings/DrawingUploadModal.test.tsx`, `src/components/drawings/DrawingPreviewPanel.test.tsx`, `src/pages/DrawingList.test.ts`).
- **fix_route:** tester — reconcile or explicitly supersede the global "Coverage achieved"/"Known gaps" sections (e.g. retitle them as "as of 2026-08-25 zone-scope rescope" or fold their still-true residual content — e.g. `DrawingList.tsx`'s zone/sub-zone *picker* UI wiring itself, as opposed to the file-type filter helper, is still untested — up into the increment-specific sections going forward).

## QA-03 — PDF preview error-state branch is untested despite the wiki DoD row claiming "loading/error states" coverage

- **where:** `src/components/drawings/DrawingPreviewPanel.test.tsx` vs. `wiki/tech/testing/per-feature/drawing.md` row: `".pdf renders via a native <iframe>... | ...PDF loading/error states | ✅ pass"`
- **what:** `DrawingPreviewPanel.tsx`'s PDF branch has a genuine error UI path (`pdfError` → `AlertTriangle` + "Failed to load preview — download to view", diff lines 232–239 of `DrawingPreviewPanel.tsx`), but no test in `DrawingPreviewPanel.test.tsx` ever sets `isError: true` on the mocked `useDrawingPdfUrl`. Only the loading state (`isLoading: true`) is exercised. The wiki's DoD-coverage-map row claims both "loading/error states" are covered and marks the row `✅ pass` — the error half of that claim is not backed by a test.
- **severity:** Medium
- **evidence:** `grep -n "isError" src/components/drawings/DrawingPreviewPanel.test.tsx` → every occurrence is `isError: false` (lines 35, 54, 63, 72); no test sets it `true`. This is an implemented-but-untested edge case, not a known production defect.
- **fix_route:** tester — add a `isError: true` test case asserting the `AlertTriangle`/"Failed to load preview — download to view" branch renders, or correct the wiki row's status/wording if this is intentionally deferred.

## QA-04 — No CI run exists for this branch (expected — pre-commit/pre-push review)

- **where:** `dev-t-drawing-pdf-upload` (local only)
- **what:** Per the task framing, this is a pre-commit review of an uncommitted working tree; the branch has never been pushed to `origin` (`git branch -r` shows no `origin/dev-t-drawing-pdf-upload`), so no CI run exists to check. Noted per checklist item 6, not treated as a failure.
- **severity:** Low/Info — expected at this stage, not a defect.
- **evidence:** `git branch -r` — branch absent from remote list; `git log --oneline -3` on `dev-t-drawing-pdf-upload` shows no local commits ahead of the last merge (`bea37d8`), confirming all changes are still uncommitted.
- **fix_route:** devops — once QA/security clear and the user approves, commit + push so CI can run before merge into `dev`/`staging`/`main`.

## QA-05 — Full-suite regression counts (backend 679/679, frontend 52/52) not independently re-run in this pass — evidence-scope caveat only

- **where:** Wiki/Notion claim: `"Backend: 679/679 passing tests, 17 pre-existing failures unchanged (4 unrelated suites). Frontend: 52/52 passing..."`
- **what:** This review re-ran only the drawings-scoped test commands specified for this dispatch (`cd backend && npx jest src/modules/drawings/` and `npx vitest run src/hooks/useDrawings.test.tsx src/components/drawings/ src/pages/DrawingList.test.ts`), not the full backend/frontend suites. The scoped runs are 100% clean (see "Independent verification" below), consistent with the claimed no-regression outcome, but the full-suite totals themselves were not independently reproduced by this reviewer.
- **severity:** Low/Info — not a finding against the implementation; a transparency note on this review's evidence scope. Consistent with the role card's normal "trust tester" posture for full-suite regression counts.
- **evidence:** See "Independent verification performed in this pass" below for exact scoped-run output.
- **fix_route:** none required.

## Independent verification performed in this pass (not findings — confirms claims accurate)

- `cd backend && npx jest src/modules/drawings/` → 2 suites (`drawing-aps.service.spec.ts`, `drawings.service.spec.ts`), **31/31 passing**, 0 failures. (3 console.error lines in the output are `drawing-aps.service.spec.ts`'s own deliberate never-throws-on-failure test scenarios logging via NestJS's Logger — expected, not real failures.)
- `npx vitest run src/hooks/useDrawings.test.tsx src/components/drawings/ src/pages/DrawingList.test.ts` → 4 files, **17/17 passing**, 0 failures.
- `cd backend && npx tsc --noEmit -p .` → clean, exit 0, zero diagnostic output.
- `npx tsc --noEmit -p .` (repo root, frontend) → clean, exit 0, zero diagnostic output.
- Diffed `git diff dev -- backend/src/modules/drawings/ src/api/drawings.ts src/hooks/useDrawings.ts src/components/drawings/ src/pages/DrawingList.tsx` line-by-line against both the Notion task's Completion Notes and the wiki's 2026-09-14 increment table — every claimed behavior traced to actual code:
  - `getLatestVersion(zoneId, subZoneId, fileType)` — `where` clause gains `file_name: { endsWith: '.${fileType}', mode: 'insensitive' }` — matches "independent version sequences" claim.
  - `LatestVersionQueryDto` (new file, read in full) — `@IsIn(['dwg','pdf'])` on a required `file_type!: 'dwg' | 'pdf'` field extending `QueryDrawingDto` — matches "required, only dwg/pdf accepted" claim exactly.
  - `useDrawingPdfUrl` — confirmed the eager `URL.revokeObjectURL()` cleanup effect described in the bug report is **absent** from the current diff (removed, not just changed) — matches "Fixed by removing the eager revoke entirely" claim. Regression test (`useDrawings.test.tsx` — "does not revoke the object URL just because the caller temporarily stops requesting it") reproduces the exact scenario described (rerender with `fileKey: null`, asserts `revokeSpy` never called with the cached URL).
  - `DrawingUploadModal.tsx` — type picker `<select>`, `handleTypeChange` clearing `stagedFiles`, `DRAWING_FORMATS`/`DRAWING_HINT` keyed by type — all present and covered by the 4 tests in `DrawingUploadModal.test.tsx` (read in full; test names/assertions match the wiki table's claims 1:1).
  - `DrawingPreviewPanel.tsx` — `isPdf()` dispatch, `#navpanes=0` iframe src suffix — confirmed both in source and in `DrawingPreviewPanel.test.tsx`'s dedicated navpanes-assertion test (`iframe.src` literal-equality check).
  - `DrawingList.tsx` — `filterDrawingsByType` exported as a pure function, browse-side type `<select>` with `useEffect` resetting to `'dwg'` on zone/sub-zone change — matches wiki's own caveat that only the pure filter helper is unit-tested (`DrawingList.test.ts`, read in full: 3 tests, all on `filterDrawingsByType` only) and the page wiring itself is not — wiki's stated gap is accurate, not overstated.
  - Backend controller/DTO changes (`drawings.controller.ts`) — `getLatestVersion` route now takes `LatestVersionQueryDto` and passes `query.file_type` through — matches "no other backend change needed" claim (upload endpoint itself untouched in the diff).
- All 4 new/untracked test files and the new DTO file confirmed present on disk with today's-date mtimes (`ls -la`), consistent with "written this session" framing.
- Confirmed via Notion (`notion-fetch`) that both the Task (T01) and Feature (F-Drawing PDF Upload + Preview) pages are `Status: Done` with Completion Notes that match the actual diff and the wiki summary — no discrepancy found between Notion's claims and the code.

## Verdict

**WARN** — 3 Medium findings (QA-01, QA-02, QA-03), all documentation/test-coverage-claim gaps, zero Critical/High findings, zero implementation-correctness concerns. All backend+frontend scoped tests pass, `tsc --noEmit` clean both sides, Notion completion notes accurately reflect the actual diff. Per role card, WARN requires explicit user confirmation before this ships — none of the three findings block correctness, but QA-02/QA-03 mean the wiki test-summary artifact (this feature's primary QA-audit artifact) currently overstates frontend coverage in two places and should be corrected before being trusted at face value by the next reviewer.
